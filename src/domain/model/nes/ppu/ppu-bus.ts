export class PpuBus {
    public readByte(address: number, wakaran: boolean = true): number {
        // TODO 実装。元がどこに実装されているのかわからないが…。
        return 0;
    }

    public generateCpuInterrupt(): void {
        // TODO 実装。元がどこに実装されているのかわからないが…。
    }

    public writeByte(address: number, data: number): void {
        // TODO 実装。元がどこに実装されているのかわからないが…。
    }
}
