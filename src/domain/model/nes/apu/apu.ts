export class Apu {
    public readRegister4015(): number {
        // TODO 実装。以下は仮実装居。
        return 0;
    }

    public writeRegister(data: number, addr: number): void {
        // TODO 実装。
    }
}
